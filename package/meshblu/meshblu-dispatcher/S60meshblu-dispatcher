#!/bin/sh
#
# Start Meshblu Dispatcher
#

start() {
        printf "Starting Meshblu Dispatcher: "
        if /etc/knot/start.sh meshblu-dispatcher
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

stop() {
        printf "Stopping Meshblu Dispatcher: "
        if /etc/knot/stop.sh meshblu-dispatcher
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
