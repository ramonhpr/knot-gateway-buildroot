#!/bin/sh
#
# Start knotctld
#

start() {
        printf "Starting knotctld: "
        if /etc/knot/start.sh knot-control
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

stop() {
        printf "Stopping knotctld: "
        if /etc/knot/stop.sh knot-control
        then
          echo "OK"
        else
          echo "FAIL"
        fi
}

restart() {
        stop
        start
}


case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?
